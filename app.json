{
  "name": "spmail",
  "description": "Email builder application with drag-and-drop interface for creating HTML emails",
  "repository": "https://github.com/jaredpalmer/react-email-workflow",
  "keywords": ["nextjs", "react", "email", "builder", "typescript"],
  "success_url": "/",
  "stack": "heroku-24",
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "env": {
    "NODE_ENV": {
      "description": "Node environment",
      "value": "production"
    },
    "NEXT_PUBLIC_APP_URL": {
      "description": "The URL of your application",
      "required": false
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "addons": [
    {
      "plan": "papertrail:choklad",
      "options": {
        "required": false
      }
    }
  ],
  "scripts": {
    "postdeploy": "echo 'Deployment complete!'"
  }
}